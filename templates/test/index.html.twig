{% extends 'base.html.twig' %}

{% block title %}Hello CGUController!{% endblock %}

{% block body %}
    {{ form_start(form) }}
        {{ form_row(form.name, { 'label': 'Nom de la figure' }) }}
        {{ form_row(form.description, { 'label': 'Description' }) }}
        {{ form_row(form.group_stunt, { 'label': 'Type de tricks' }) }}
        {{ form_row(form.tempMedias, { 'label': "Médias" }) }}
        <button id="more_media" class="btn btn-primary"><i class="bi bi-plus"></i></button>
        <div class="text-end">
            {{ form_row(form.submit, { 'label': 'Envoyer' }) }}
        </div>
    {{ form_end(form) }}
{% endblock %}

{% block script %}
    <script src="{{ asset('assets/js/tricks_embed.js') }}"></script>
{% endblock %}
